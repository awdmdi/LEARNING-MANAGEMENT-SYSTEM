#ifndef VALIDATION_H
#define VALIDATION_H

#include <string>
using namespace std;

bool isValidRoll(const string& r);
bool isValidGmail(const string& g);
bool isStrongPassword(const string& p);

#endif
